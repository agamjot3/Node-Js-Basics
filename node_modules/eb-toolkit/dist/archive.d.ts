import { Application } from "./application";
import { S3 } from 'aws-sdk';
export declare class Archive {
    private readonly application;
    private readonly s3;
    constructor(application: Application, s3: S3);
    checkUploaded(applicationName: any, versionLabel: any): Promise<boolean>;
    upload({ applicationName, filePath, versionLabel, }: {
        applicationName: string;
        filePath: string;
        versionLabel?: string;
    }): Promise<string | undefined>;
    private uploadToS3;
    private zipFolder;
}
